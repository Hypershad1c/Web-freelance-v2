"use client";

import { useState, useTransition } from "react";
import { useRouter } from "next/navigation";
import { FileUp, LoaderCircle, MessageCircleMore, Store } from "lucide-react";
import { addCrmDocument, createCrmSellerCase, logCrmCommunication } from "@/lib/actions/crm-operations";

type Deal={id:string;title:string}; type Property={id:string;title:string;reference:string};
function useFormAction(action:(data:FormData)=>Promise<void>){const router=useRouter();const[pending,startTransition]=useTransition();const[error,setError]=useState<string|null>(null);function submit(data:FormData){setError(null);startTransition(async()=>{try{await action(data);router.refresh()}catch(e){setError(e instanceof Error?e.message:"Action impossible")}})}return{pending,error,submit}}
export function CrmSalesTools({contactId,deals,properties}:{contactId:string;deals:Deal[];properties:Property[]}){const comm=useFormAction((data)=>logCrmCommunication(contactId,data));const doc=useFormAction((data)=>addCrmDocument(contactId,data));const seller=useFormAction((data)=>createCrmSellerCase(contactId,data));return <section className="grid gap-5"><Tool title="Journal des échanges" icon={MessageCircleMore}><form action={comm.submit} className="grid gap-3"><div className="grid grid-cols-2 gap-3"><select name="channel" className="domify-select"><option value="PHONE">Appel</option><option value="EMAIL">Email</option><option value="WHATSAPP">WhatsApp</option><option value="SMS">SMS</option><option value="IN_APP">Interne</option></select><select name="direction" className="domify-select"><option value="OUTBOUND">Sortant</option><option value="INBOUND">Entrant</option></select></div><input name="subject" className="domify-select" placeholder="Objet facultatif"/><select name="dealId" className="domify-select"><option value="">Sans opportunité</option>{deals.map(d=><option key={d.id} value={d.id}>{d.title}</option>)}</select><textarea name="body" required rows={3} className="rounded-xl border border-domify-dark/10 px-3 py-2 text-sm" placeholder="Résumez l’échange, le résultat et la prochaine action…"/>{comm.error&&<p className="text-xs text-red-700">{comm.error}</p>}<Submit pending={comm.pending} label="Enregistrer l’échange"/></form></Tool><Tool title="Coffre documentaire" icon={FileUp}><form action={doc.submit} className="grid gap-3"><input name="name" required className="domify-select" placeholder="Nom du document"/><input name="url" required type="url" className="domify-select" placeholder="URL sécurisée du document"/><select name="type" className="domify-select"><option value="IDENTITY">Identité</option><option value="MANDATE">Mandat</option><option value="TITLE_DEED">Titre de propriété</option><option value="FINANCING">Financement</option><option value="OFFER">Offre</option><option value="CONTRACT">Contrat</option><option value="OTHER">Autre</option></select><select name="dealId" className="domify-select"><option value="">Sans opportunité</option>{deals.map(d=><option key={d.id} value={d.id}>{d.title}</option>)}</select><select name="propertyId" className="domify-select"><option value="">Sans bien</option>{properties.map(p=><option key={p.id} value={p.id}>{p.reference} — {p.title}</option>)}</select><textarea name="notes" rows={2} className="rounded-xl border border-domify-dark/10 px-3 py-2 text-sm" placeholder="Note interne"/>{doc.error&&<p className="text-xs text-red-700">{doc.error}</p>}<Submit pending={doc.pending} label="Ajouter au coffre"/></form></Tool><Tool title="Parcours vendeur" icon={Store}><form action={seller.submit} className="grid gap-3"><input name="title" required className="domify-select" placeholder="Ex. Vente villa à Souissi"/><input name="estimatedValue" type="number" min="0" className="domify-select" placeholder="Estimation (MAD)"/><select name="propertyId" className="domify-select"><option value="">Bien à créer ou non rattaché</option>{properties.map(p=><option key={p.id} value={p.id}>{p.reference} — {p.title}</option>)}</select><input name="nextActionAt" type="datetime-local" className="domify-select"/><textarea name="notes" rows={2} className="rounded-xl border border-domify-dark/10 px-3 py-2 text-sm" placeholder="Contexte de la valorisation ou du mandat"/>{seller.error&&<p className="text-xs text-red-700">{seller.error}</p>}<Submit pending={seller.pending} label="Ouvrir le dossier vendeur"/></form></Tool></section>}
function Tool({title,icon:Icon,children}:{title:string;icon:typeof Store;children:React.ReactNode}){return <article className="rounded-[1.35rem] border border-domify-dark/8 bg-white p-5 shadow-sm"><div className="flex items-center gap-2 text-domify-primary"><Icon size={18}/><h2 className="font-display text-xl font-semibold">{title}</h2></div><div className="mt-4">{children}</div></article>};function Submit({pending,label}:{pending:boolean;label:string}){return <button disabled={pending} className="pressable rounded-xl bg-domify-primary px-4 py-3 text-sm font-semibold text-white">{pending&&<LoaderCircle size={14} className="mr-2 inline animate-spin"/>}{label}</button>}
